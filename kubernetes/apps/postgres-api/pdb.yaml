apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: postgres-api
  namespace: apps
  labels:
    app: postgres-api
    team: platform
    environment: lab
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: postgres-api
