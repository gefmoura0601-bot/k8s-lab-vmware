apiVersion: v1
kind: Service
metadata:
  name: postgres-api
  namespace: apps
  labels:
    app: postgres-api
    team: platform
    environment: lab
spec:
  selector:
    app: postgres-api
  ports:
    - name: http
      port: 80
      targetPort: http
