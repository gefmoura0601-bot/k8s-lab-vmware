apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: postgres
  namespace: databases
  labels:
    app: postgres
    team: platform
    environment: lab
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: postgres
