apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-lab
  namespace: nginx-lab
spec:
  ingressClassName: nginx
  rules:
    - host: nginx.lab.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx-lab
                port:
                  number: 80
