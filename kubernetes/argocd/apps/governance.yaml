apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: governance
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: apps
  source:
    repoURL: https://github.com/gefmoura0601-bot/k8s-lab-vmware.git
    targetRevision: main
    path: kubernetes/platform/governance/base
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
